---
phase: 01-infra-wiring-local-smoke-test
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - docs/rollback.md
  - README.md
autonomous: false
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03

must_haves:
  truths:
    - "A new operations runbook exists at docs/rollback.md covering: tier-swap rollback procedure, Phase 2 key-rotation procedure, and the empirical sandbox-behavior baseline (populated from Plan 02's approved summary)."
    - "The root README.md contains a one-line pointer to docs/rollback.md so the runbook is discoverable without bloating the README."
    - "CONFIG-02 (rotate LIVEAVATAR_API_KEY GitHub secret to the free-tier account key) is explicitly documented as DEFERRED TO PHASE 2 inside the runbook (D-09) — not silently dropped."
    - "CONFIG-01 verification (LIVE_AVATAR_IS_SANDBOX=true and LIVE_AVATAR_AVATAR_ID=dd73ea75-... confirmed present in GitHub repo variables) has been performed by the operator via the GitHub UI."
    - "No backend Python file is modified by this plan; no Terraform/workflow file is modified by this plan (those were Plan 01)."
  artifacts:
    - path: "docs/rollback.md"
      provides: "Operations runbook with 6 headed sections (Overview, Trigger criteria, Procedure, Verification, Phase 2 key-rotation procedure, Sandbox Behavior Baseline appendix)"
      min_lines: 70
      contains: "## Rollback procedure"
    - path: "docs/"
      provides: "New operations-docs directory (created as part of this plan)"
    - path: "README.md"
      provides: "One-line pointer under an Operations/Rollback subsection"
      contains: "docs/rollback.md"
  key_links:
    - from: "README.md"
      to: "docs/rollback.md"
      via: "Markdown link"
      pattern: "docs/rollback.md"
    - from: "docs/rollback.md tier-swap procedure"
      to: "Plan 01's wired TF_VAR_live_avatar_is_sandbox"
      via: "Flip LIVE_AVATAR_IS_SANDBOX GitHub Variable to false then rerun Deploy workflow"
      pattern: "LIVE_AVATAR_IS_SANDBOX"
    - from: "docs/rollback.md Sandbox Behavior Baseline appendix"
      to: "Plan 02's approved operator-run summary"
      via: "Verbatim paste of the markdown block emitted by scripts/smoke-liveavatar.py"
      pattern: "Sandbox Behavior Baseline"
---

<objective>
Author the Phase 1 operations runbook at `docs/rollback.md` (new file) and add a one-line pointer from the root `README.md`. The runbook covers: (a) tier-swap rollback procedure using the now-wired `LIVE_AVATAR_IS_SANDBOX` GitHub Variable (depends on Plan 01), (b) Phase 2 key-rotation procedure where CONFIG-02 actually ships (per D-09's deferral), and (c) a dated "Sandbox Behavior Baseline" appendix populated verbatim from Plan 02's operator-approved smoke-run summary. Also confirms CONFIG-01 (GitHub repository variables present) by the operator via the GitHub UI.

Purpose: ensure that (i) the tier switch is reversible via a documented 5-minute GitHub-Variable flip, (ii) the deferred CONFIG-02 is preserved in writing so Phase 2 does not re-discover the need to rotate the key, (iii) the empirical sandbox behaviors captured in Plan 02 are stored with the runbook that operators actually read, and (iv) CONFIG-01 is not implicitly assumed.

Output: `docs/rollback.md` (~80-120 lines, 6 headed sections), a one-line pointer line in `README.md`, and an operator-confirmed CONFIG-01 check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infra-wiring-local-smoke-test/01-CONTEXT.md
@.planning/phases/01-infra-wiring-local-smoke-test/01-01-SUMMARY.md
@.planning/phases/01-infra-wiring-local-smoke-test/01-02-SUMMARY.md

@README.md
@infra/terraform/variables.tf
@.github/workflows/deploy-azure.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/rollback.md and add the one-line README pointer (CONFIG-03, documents CONFIG-02 deferral per D-09)</name>
  <files>docs/rollback.md, README.md</files>
  <read_first>
    - README.md (full — to find a sensible insertion point for the one-line pointer; add an "Operations" section near the end)
    - .planning/phases/01-infra-wiring-local-smoke-test/01-CONTEXT.md decisions D-09, D-11, D-12, D-14 (locked decisions driving section order and content)
    - .planning/phases/01-infra-wiring-local-smoke-test/01-01-SUMMARY.md (for the concrete TF_VAR / env / fallback values to reference in the tier-flip procedure)
    - .planning/phases/01-infra-wiring-local-smoke-test/01-02-SUMMARY.md (for the verbatim `## Sandbox Behavior Baseline` block to paste as the appendix)
    - .github/workflows/deploy-azure.yml (READ-ONLY — for the workflow name `Deploy to Azure` to reference in the "rerun workflow" step)
  </read_first>
  <action>
**Edit 1 — Create `docs/` directory (new) and write `docs/rollback.md`** with these six sections in this exact order (per D-12 + D-14). Use standard GitHub-flavoured markdown; keep lines under ~100 chars where reasonable. The literal content to write is:

    # Operations Runbook — LiveAvatar Tier Rollback

    **Scope:** Emergency rollback of the LiveAvatar free-tier switch (milestone:
    "LiveAvatar Free-Tier Switch"). This runbook is the canonical reference for
    operators needing to flip production back to the paid tier, for the Phase 2
    API-key rotation, and for the empirical sandbox-behavior baseline captured
    during Phase 1's local smoke test.

    ---

    ## Overview

    Production `dimangulov.space` runs the LiveAvatar sandbox tier by default
    (`LIVEAVATAR_IS_SANDBOX=true`, `LIVEAVATAR_SESSION_MODE=LITE`, Wayne avatar
    UUID `dd73ea75-1218-4ef3-92ce-606d5f7fbc0a`). The sandbox tier consumes zero
    LiveAvatar credits but caps each session at ~60-120 seconds and permits only
    the Wayne stock avatar.

    If the sandbox tier proves unusable in production, the tier switch is
    reversible via a single GitHub Actions repository variable change followed by
    a workflow rerun — **no code change required**.

    ---

    ## Trigger criteria

    Flip back to the paid tier (`LIVEAVATAR_IS_SANDBOX=false`) if any of these
    are observed on the live site:

    - The avatar fails to render and `/session` returns non-200 for >15 minutes.
    - LiveAvatar provider dashboard shows unexpected credit consumption despite
      `is_sandbox=true` — indicates the sandbox flag is not reaching the provider.
    - The ~60-120 s sandbox cap causes a user-visible UX regression that
      outweighs the zero-cost benefit.
    - A legal or compliance blocker emerges specific to the sandbox tier (e.g.,
      Wayne likeness usage rights change).

    ---

    ## Rollback procedure

    Estimated time: **5 minutes** (GitHub Variable edit + ~3 minute pipeline run).

    1. Open `https://github.com/<owner>/<repo>/settings/variables/actions`.
    2. Edit the repository variable `LIVE_AVATAR_IS_SANDBOX` and set its value
       to `false`.
    3. Go to `Actions` → `Deploy to Azure` → click `Run workflow` → select
       `main` → `Run workflow`.
    4. Wait for all four jobs to go green: `terraform-infra` →
       `build-backend` → `deploy-backend` → `deploy-frontend`.
    5. The new Container App revision will have `LIVEAVATAR_IS_SANDBOX=false`
       sourced from `TF_VAR_live_avatar_is_sandbox`, which falls back to `'true'`
       only when the repo variable is absent — setting it explicitly to `false`
       overrides that default.

    **Important:** if the paid LiveAvatar API key has already been revoked
    (final Phase 3 step, `DEPLOY-05`), the rollback also requires a new paid key
    in the `LIVEAVATAR_API_KEY` GitHub secret. Do NOT attempt the rollback
    without a valid paid key in place.

    ---

    ## Verification

    After the pipeline goes green:

    - `curl -s https://<backend-url>/health` returns `{"status":"ok"}`.
    - Load `https://dimangulov.space`, open the dev console, ask a question.
    - The avatar renders without the `[ POC — Connect LiveAvatar API ]` mock
      watermark.
    - LiveAvatar provider dashboard shows new credit consumption on the next
      session (proves `is_sandbox=false` reached the provider).

    If any of these fail, capture the backend container logs
    (`az containerapp logs show --name aicv-prod-backend --resource-group rg-aicv-prod`)
    and the Terraform Apply job output before further action.

    ---

    ## Phase 2 key-rotation procedure (CONFIG-02 — deferred from Phase 1)

    **Why this is here:** Phase 1 intentionally kept the existing paid LiveAvatar
    API key in place while running the sandbox smoke test (decision D-08 in
    `.planning/phases/01-infra-wiring-local-smoke-test/01-CONTEXT.md`). Rotating
    the key to a free-tier account key is Phase 2's responsibility and is
    documented here so the Phase 2 executor inherits a written procedure rather
    than re-discovering the need.

    Procedure:

    1. In the LiveAvatar provider dashboard, create or confirm a free-tier
       account and generate an API key for it.
    2. On GitHub, cancel any in-flight `Deploy to Azure` workflow runs to avoid
       racing an old key into a new revision.
    3. Update the `LIVEAVATAR_API_KEY` GitHub **secret** (not variable) at
       `https://github.com/<owner>/<repo>/settings/secrets/actions` with the new
       free-tier key.
    4. Push a no-op commit (or use `workflow_dispatch`) to trigger the pipeline.
    5. After the new revision is healthy, verify in the LiveAvatar provider
       dashboard that the new session is attributed to the free-tier account
       and shows 0 credits consumed.
    6. The old paid key is revoked as the final step of Phase 3
       (`DEPLOY-05`) — not during Phase 2.

    ---

    ## Sandbox Behavior Baseline (observed YYYY-MM-DD)

    *Populate this appendix verbatim from Plan 02's approved smoke-test summary
    block. Do not re-run the script here; paste the block produced by
    `scripts/smoke-liveavatar.py` during Plan 02 execution.*

    <!-- PASTE BEGIN: Plan 02 Sandbox Behavior Baseline block -->
    <!-- PASTE END -->

    **Source of truth:** `.planning/phases/01-infra-wiring-local-smoke-test/01-02-SUMMARY.md`.
    Research file `.planning/research/PITFALLS.md` is NOT mutated — this
    appendix is the live operational record (per decision D-14).

Then locate the `<!-- PASTE BEGIN -->` / `<!-- PASTE END -->` markers and replace the block between them (inclusive or exclusive — executor's choice, both are acceptable if the final heading `## Sandbox Behavior Baseline` appears exactly once) with the verbatim `## Sandbox Behavior Baseline (observed YYYY-MM-DD)` markdown block captured by the operator in Plan 02's approval signal (see `01-02-SUMMARY.md`). Preserve the header and table exactly as emitted by the script — do not reformat, do not edit the status values, do not renumber.

**Edit 2 — `README.md`:** Append a new section at the end of the file (after whatever the current last non-empty section is). Do not modify any existing section. The exact new section to append is:

    ---

    ## Operations

    Rollback procedure, Phase 2 key-rotation procedure, and the sandbox-behavior
    baseline: see [docs/rollback.md](./docs/rollback.md).

(The leading `---` separator matches the existing horizontal-rule style already used in `README.md` between sections.)

**Scope discipline (D-13):** do NOT modify any file under `backend/`; do NOT modify Terraform or workflow files in this task (those are Plan 01's domain). Do NOT modify `.planning/REQUIREMENTS.md` or `.planning/research/PITFALLS.md` (per D-14).
  </action>
  <verify>
    <automated>test -f docs/rollback.md &amp;&amp; grep -q "## Rollback procedure" docs/rollback.md &amp;&amp; grep -q "Phase 2 key-rotation procedure" docs/rollback.md &amp;&amp; grep -q "Sandbox Behavior Baseline" docs/rollback.md &amp;&amp; grep -q "LIVE_AVATAR_IS_SANDBOX" docs/rollback.md &amp;&amp; grep -q "DEPLOY-05" docs/rollback.md &amp;&amp; grep -q "docs/rollback.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/` directory exists.
    - `docs/rollback.md` exists and is >=70 lines.
    - `docs/rollback.md` contains ALL of these literal strings exactly once (or more) each: `# Operations Runbook`, `## Overview`, `## Trigger criteria`, `## Rollback procedure`, `## Verification`, `Phase 2 key-rotation procedure`, `## Sandbox Behavior Baseline`, `LIVE_AVATAR_IS_SANDBOX`, `LIVEAVATAR_API_KEY`, `D-08`, `DEPLOY-05`, `dd73ea75-1218-4ef3-92ce-606d5f7fbc0a`.
    - `docs/rollback.md` contains the verbatim `## Sandbox Behavior Baseline (observed YYYY-MM-DD)` markdown block produced by Plan 02 (the date is the actual date of the operator's run, not the literal placeholder).
    - `docs/rollback.md` does NOT contain any literal LiveAvatar API key value (neither paid nor free-tier) — runbook examples use placeholder `<...>` notation only.
    - `README.md` contains a new `## Operations` section with the exact link `[docs/rollback.md](./docs/rollback.md)`.
    - `README.md` existing sections (`# Interactive Digital Twin CV`, `## Architecture Overview`, `## Prerequisites`, `## Quick Start`) are byte-identical to before the edit.
    - No file under `backend/` is modified by this task.
    - No file under `infra/` or `.github/` is modified by this task.
    - No file under `.planning/research/` is modified by this task.
  </acceptance_criteria>
  <done>`docs/rollback.md` exists with all six required headed sections, Plan 02's baseline block pasted verbatim, and README contains a one-line pointer under an Operations section. No unrelated files touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies CONFIG-01 (GitHub repository variables present)</name>
  <files>(no file modifications — operator verification of existing GitHub repository variables via the web UI)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the CONFIG-01 acceptance wording)
    - .planning/phases/01-infra-wiring-local-smoke-test/01-CONTEXT.md decisions D-08, D-09 (to understand why CONFIG-02 is NOT checked here)
    - docs/rollback.md (as written in Task 1 — the verification step references the same variable name)
  </read_first>
  <what-built>Plan 01 wired two new `TF_VAR_live_avatar_*` entries in `.github/workflows/deploy-azure.yml` that read from `vars.LIVE_AVATAR_IS_SANDBOX` and `vars.LIVE_AVATAR_SESSION_MODE`. Plan 03 Task 1 wrote the rollback runbook that assumes those variables are set. This task verifies, via the GitHub web UI, that CONFIG-01's two repository variables are actually configured on the repo.</what-built>
  <action>
Operator-only verification task — no file changes. Follow the `<how-to-verify>` steps below, using the GitHub web UI, to confirm CONFIG-01's two repository variables are present with the correct values, and that the `LIVEAVATAR_API_KEY` secret exists (without rotating it — that is Phase 2's responsibility per D-09).

The acceptance signal is the operator's free-text approval in the `<resume-signal>` listing the observed variable values.
  </action>
  <how-to-verify>
1. Open `https://github.com/<owner>/<repo>/settings/variables/actions` in a browser (requires repo admin or maintainer permissions).
2. Confirm the following **repository variables** (not secrets) exist with these values:
   - `LIVE_AVATAR_IS_SANDBOX` = `true`
   - `LIVE_AVATAR_AVATAR_ID` = `dd73ea75-1218-4ef3-92ce-606d5f7fbc0a`
3. (Optional, recommended) Also confirm `LIVE_AVATAR_SESSION_MODE` = `LITE` is present. If it is absent, the workflow fallback `'LITE'` (added in Plan 01) will apply, so its absence is non-blocking for Phase 1 but should be added before Phase 2 for explicitness.
4. Confirm the `LIVEAVATAR_API_KEY` GitHub **secret** is present (existing paid key — its rotation to a free-tier key is deferred to Phase 2 per D-09).
5. Do NOT rotate `LIVEAVATAR_API_KEY` in this phase. That is Phase 2's first step (documented in the runbook).

Expected outcome: both CONFIG-01 variables present with the exact values above. If either is missing or has a different value, set them now via the GitHub UI before proceeding to Phase 2.
  </how-to-verify>
  <verify>
    <automated>MISSING — this checkpoint is an operator verification of GitHub repository configuration (not a file-based check). The operator's resume-signal (listing the observed variable values) is the acceptance evidence.</automated>
  </verify>
  <acceptance_criteria>
    - Operator has viewed `https://github.com/<owner>/<repo>/settings/variables/actions` and confirmed the two CONFIG-01 variable values.
    - If either variable was missing, operator has set it via the GitHub UI before resuming.
    - Operator has confirmed the `LIVEAVATAR_API_KEY` secret exists (value not inspected — GitHub masks secrets).
    - Operator has NOT rotated the `LIVEAVATAR_API_KEY` secret (that is a Phase 2 action per D-09).
  </acceptance_criteria>
  <done>Operator has confirmed CONFIG-01 variables are set correctly in the GitHub UI, the `LIVEAVATAR_API_KEY` secret exists, and no rotation of the key has been performed in this phase.</done>
  <resume-signal>Type "approved: CONFIG-01 verified, variables present" or describe which variables were missing/wrong and what you set them to.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 02 summary → docs/rollback.md (paste operation) | Executor pastes a verbatim block from `01-02-SUMMARY.md` into `docs/rollback.md`. The block is markdown-only, but the paste could in principle carry unintended content. |
| `docs/rollback.md` → Git → GitHub repo → public web | The repo appears to be publicly-readable (live site `dimangulov.space` is public). Runbook content is world-readable. |
| README.md pointer → `docs/rollback.md` | Pure Markdown link; no privilege boundary crossed. |
| GitHub web UI → operator's eyes (CONFIG-01 check) | Operator reads repository-variable values in a browser; the session is authenticated with GitHub. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | Runbook content published to public repo could leak operational detail (backend URL, resource group name, secret *names*). | accept | Backend URL and resource group (`rg-aicv-prod`) are already in `README.md` and Terraform files. Secret *names* (e.g., `LIVEAVATAR_API_KEY`) are not secrets themselves. No secret *values* appear in the runbook. |
| T-03-02 | Information disclosure | Pasted `## Sandbox Behavior Baseline` block could contain a `session_token` or `ws_token` value if the Plan 02 script emitted one. | mitigate | Plan 02's `emit_summary` (Plan 02 Task 1 action spec) explicitly allows only: check IDs, status markers, elapsed times, and textual categories. No raw response bodies. Acceptance criterion re-verifies by grepping the final runbook for the pattern `token|bearer|api[-_]key` (case-insensitive) before the plan is considered done. |
| T-03-03 | Information disclosure | CONFIG-01 verification step leaks the avatar UUID in the runbook. | accept | The Wayne UUID `dd73ea75-...` is a public LiveAvatar sandbox identifier (documented at `docs.liveavatar.com`). Not a secret. |
| T-03-04 | Tampering | Operator sets `LIVE_AVATAR_IS_SANDBOX` to an unexpected value (e.g., `True` with capital T, or `1`) while "verifying" CONFIG-01. | mitigate | `backend/config.py:60` does `.lower() == "true"`, so `"True"`, `"TRUE"`, and `"true"` all work; `"1"` would silently resolve to `false`. Runbook explicitly documents the expected value as lowercase `true`. |
| T-03-05 | Repudiation | Operator's CONFIG-01 approval is a free-text signal; no audit trail that the variables were actually set. | accept | GitHub's own audit log records who set which repo variable when. The runbook is not the audit record. |
| T-03-06 | Spoofing | Copy-paste from Plan 02 summary could include HTML/script content if the smoke script was tampered with. | mitigate | The paste is into a `.md` file (not rendered as HTML in the repo view on github.com — which sanitizes). Acceptance criterion re-verifies the runbook is plain markdown (no `<script`, no `javascript:` URLs) via grep. |
| T-03-07 | Information disclosure | CONFIG-02 deferral text in the runbook explains how to rotate the key; if misread, an inexperienced operator might attempt rotation during Phase 1. | mitigate | Section heading is explicit: `## Phase 2 key-rotation procedure (CONFIG-02 — deferred from Phase 1)`. Step 6 in the procedure explicitly links revocation to `DEPLOY-05` (final Phase 3 step). |

All threats in this plan are low severity (documentation + operator verification). The one non-trivial control is T-03-02 (token leakage via pasted summary) — mitigated at source in Plan 02's script spec.
</threat_model>

<verification>
- `test -f docs/rollback.md` returns 0.
- `wc -l docs/rollback.md` returns a number >= 70.
- `grep -c "## Rollback procedure" docs/rollback.md` returns >= 1.
- `grep -c "Phase 2 key-rotation procedure" docs/rollback.md` returns >= 1.
- `grep -c "Sandbox Behavior Baseline" docs/rollback.md` returns >= 1.
- `grep -c "DEPLOY-05" docs/rollback.md` returns >= 1.
- `grep -c "dd73ea75-1218-4ef3-92ce-606d5f7fbc0a" docs/rollback.md` returns >= 1.
- `grep -Ei "(session_token|ws_token|bearer |X-Api-Key:)" docs/rollback.md` returns 0 matches (T-03-02 re-verification — no token values leaked via paste).
- `grep -Ei "<script|javascript:" docs/rollback.md` returns 0 matches (T-03-06 re-verification — plain markdown).
- `grep -c "docs/rollback.md" README.md` returns >= 1.
- `git diff --name-only` for this plan includes ONLY `docs/rollback.md` (added) and `README.md` (modified). No `backend/*`, no `infra/*`, no `.github/*`, no `.planning/research/*`.
- Operator has signalled "approved: CONFIG-01 verified" (Task 2 resume signal).
</verification>

<success_criteria>
- CONFIG-01 verified by operator (both repo variables present).
- CONFIG-02 explicitly documented as deferred to Phase 2 inside the runbook (not silently dropped).
- CONFIG-03 satisfied: runbook exists with tier-swap rollback procedure, Phase 2 key-rotation procedure, and sandbox-behavior baseline appendix.
- README discoverability satisfied: one-line pointer under an Operations section.
- Zero edits to backend Python files, Terraform files, workflow files, or research files (scope-discipline per D-13, D-14).
- Pasted baseline block contains no token values or raw auth headers (T-03-02 / T-03-06).
</success_criteria>

<output>
After completion, create `.planning/phases/01-infra-wiring-local-smoke-test/01-03-SUMMARY.md` documenting:
- The six runbook sections written (with a one-line summary of each)
- Whether Plan 02's baseline block was pasted verbatim or manually edited (answer MUST be "verbatim")
- The exact CONFIG-01 variable values observed by the operator in the GitHub UI (e.g., `LIVE_AVATAR_IS_SANDBOX=true`, `LIVE_AVATAR_AVATAR_ID=dd73ea75-...`)
- Confirmation that no out-of-scope files were modified (list the full set of files actually touched: expect exactly `docs/rollback.md` added and `README.md` modified)
- A one-line pointer noting that CONFIG-02 rotation is the first step of Phase 2 and is documented in `docs/rollback.md`.
</output>
